---
interface Props {
    displayText: string;
    svgPath: string;
    id: string;
    link: string;
}
const {displayText, svgPath, id, link} = Astro.props;
---


<a href={link} target="_blank" class="icon" id={id}>
    <!-- TODO: add alt -->
    <img src={svgPath} alt="Icon" width="40" height="40">
    <p>{displayText}</p>
</a>

<style>
    .icon {
	text-decoration: none;
	background: none;
	border: none;
	cursor: pointer;
	color: white;
	max-width: 150px;
	text-align: center;
	padding: 1rem;
	transition: transform 100ms ease-in;
	img {
	    margin: auto;
	}
	p {
	    margin-block: 0;
	}
	&:hover {
	    transform: scale(1.2);
	}
    }
</style>
